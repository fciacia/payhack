import 'package:flutter/material.dart';

class RegisterBizIDScreen extends StatelessWidget {
  const RegisterBizIDScreen({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Register BizID')),
      body: const Center(child: Text('Register BizID Screen')),
    );
  }
}